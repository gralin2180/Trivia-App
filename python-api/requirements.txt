# Core (required)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
python-dotenv>=1.0.0

# Supabase (save decks / verify JWT)
supabase>=2.10.0

# Optional rich scrapers (install if you want Crawl4AI / ScrapeGraphAI)
# crawl4ai>=0.4.0
# scrapegraphai>=1.26.0
# playwright  # required by crawl4ai — then: playwright install
